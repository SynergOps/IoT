# 08_Serial_Communication - Σειριακή Επικοινωνία

## Περιγραφή
Χρησιμοποιεί το Serial Monitor για να στέλνει και να λαμβάνει δεδομένα μεταξύ Arduino και υπολογιστή. Ελέγχει το **εξωτερικό LED (Pin 9)** που χρησιμοποιήσαμε στο προηγούμενο μάθημα, στέλνοντας '1' (ON) ή '0' (OFF).

## Γιατί είναι σημαντική η Σειριακή Επικοινωνία;

Μέχρι τώρα, το Arduino μας ήταν "κλειστό κουτί" - φορτώναμε κώδικα και το παρακολουθούσαμε μόνο από το LED. 
Αλλά τι γίνεται αν θέλουμε:

- 📊 Να **δούμε τιμές** από αισθητήρες σε πραγματικό χρόνο
- 🐛 Να **εντοπίσουμε λάθη** στον κώδικα (debugging)
- 🎮 Να **ελέγξουμε** το Arduino από τον υπολογιστή
- 📡 Να **επικοινωνήσουμε** με άλλες συσκευές (Bluetooth, Wi-Fi modules)

Η απάντηση: **Σειριακή Επικοινωνία!** 

Είναι η "γέφυρα" που συνδέει το Arduino με τον έξω κόσμο. Κάθε φορά που συνδέετε το καλώδιο USB, δεν το χρησιμοποιείτε μόνο για ρεύμα - δημιουργείται και μια **αμφίδρομη επικοινωνία** για ανταλλαγή δεδομένων.

## Πού χρησιμοποιείται σε υλοποίηση IoT;
*   **Έλεγχος μέσω Bluetooth/Wi-Fi:** Μονάδες όπως το HC-05 (Bluetooth) ή το ESP8266 συνδέονται σειριακά με τον μικροελεγκτή. Όταν πατάτε "ON" στο κινητό σας, η εντολή φτάνει σειριακά και ανάβει το φως.
*   **Smart Home Hubs:** Ο κεντρικός επεξεργαστής στέλνει σειριακές εντολές στο ραδιο-τσίπ (Zigbee/Z-Wave) για να ανάψει τις έξυπνες λάμπες του σπιτιού.
*   **Δοκιμή Παραγωγής (QC):** Στο εργοστάσιο, ρομποτικά συστήματα στέλνουν εντολές μέσω σειριακής θύρας για να ανάψουν όλα τα LED της συσκευής και να επιβεβαιώσουν ότι λειτουργούν σωστά.

## Μαθησιακοί Στόχοι
- Κατανόηση σειριακής επικοινωνίας (UART)
- Χρήση `Serial.begin()`, `Serial.print()`, `Serial.read()`
- Debugging με Serial Monitor
- Διαδραστικός έλεγχος του Arduino από τον υπολογιστή

## Σχεδιάγραμμα Κυκλώματος

```
           ┌─────────────────┐         ╔═══════════════╗
           │   Arduino Uno   │  USB    ║   Computer    ║
           │                 ├─────────║               ║
  ┌────────┤ D9 (PWM)        │         ║  Serial       ║
  │        │                 │         ║  Monitor      ║
  │    ┌───┤ GND             │         ║  (9600 baud)  ║
  │    │   └─────────────────┘         ╚═══════════════╝
  │    │
  │   ─┴─  LED
  │   ▲ ▲
  │    │
  └────ww────┐
     220Ω    │
             │
  (Όπως στο 07_PWM_Fade)
```

## Υλικά που Χρειάζονται
- 1× Arduino Uno
- 1× LED (οποιοδήποτε χρώμα)
- 1× Αντίσταση 220Ω (Κόκκινο-Κόκκινο-Καφέ 🔴🔴🟤)
- 1× Breadboard & Καλώδια
- 1× Καλώδιο USB

**Σημείωση:** Αν δεν έχετε το κύκλωμα, μπορείτε να αλλάξετε τον κώδικα σε `LED_BUILTIN` (Pin 13).

**Το USB καλώδιο χρησιμεύει για δύο σκοπούς:**
1. Τροφοδοσία (5V)
2. Σειριακή επικοινωνία (RX/TX)

## Τι είναι η Σειριακή Επικοινωνία;

**Serial (UART)** = Universal Asynchronous Receiver-Transmitter

### Βασική Ιδέα
Η σειριακή επικοινωνία στέλνει δεδομένα **ένα bit τη φορά** μέσω ενός καλωδίου.

```
"Hello" → 01001000 01100101 01101100 01101100 01101111
            H        e        l        l        o
            (ένα-ένα bit στη σειρά)
```

### Πώς Συνδέονται Arduino & Υπολογιστής;

```
Arduino        USB         Computer
  TX  ─────────────────►   RX  (Transmit → Receive)
  RX  ◄─────────────────   TX  (Receive ← Transmit)
 GND  ◄────────────────►  GND  (Κοινή γείωση)
 
 TX = Transmit (Εκπομπή)
 RX = Receive (Λήψη)
```

**Σημαντικό:** Το TX του ενός συνδέεται με το RX του άλλου (σταυρωτά)!

### Baud Rate (Ταχύτητα)

**Baud Rate** = Πόσα bits ανταλλάσσονται ανά δευτερόλεπτο

- **9600 baud** = 9,600 bits/sec ≈ **960 χαρακτήρες/sec** (συνηθέστερο)
- **115200 baud** = 115,200 bits/sec ≈ **11,520 χαρακτήρες/sec** (πιο γρήγορο)

⚠️ **Κρίσιμο:** Το baud rate πρέπει να **ταιριάζει** και στα δύο άκρα!

```
Arduino: Serial.begin(9600);  ←→  Serial Monitor: 9600 baud
         (στον κώδικα)                 (dropdown μενού)
         
         ✅ Ταιριάζουν → Σωστή επικοινωνία
         ❌ Διαφορετικά → Gibberish (!@#$%^&*)
```

### Ροή Επικοινωνίας (Communication Flow)

```
┌─────────────────────────────────────────────────────────────┐
│  1. Χρήστης πληκτρολογεί '1' στο Serial Monitor            │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  2. Υπολογιστής στέλνει byte '1' (0x31) μέσω USB           │
└────────────────────┬────────────────────────────────────────┘
                     │ (TX → RX)
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  3. Arduino: Serial.available() επιστρέφει 1 (έχει δεδομένα)│
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  4. Arduino: char c = Serial.read() → c = '1'              │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  5. Arduino: if (c == '1') → digitalWrite(LED, HIGH)        │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  6. Arduino: Serial.println("LED ON") → στέλνει απάντηση   │
└────────────────────┬────────────────────────────────────────┘
                     │ (RX ← TX)
                     ▼
┌─────────────────────────────────────────────────────────────┐
│  7. Serial Monitor εμφανίζει: "LED ON"                      │
└─────────────────────────────────────────────────────────────┘
```

## Οδηγίες Χρήσης

### Βήμα προς Βήμα:
1. Συνδέστε το Arduino με USB
2. Ανεβάστε το `serial_led.ino`
3. Ανοίξτε το Serial Monitor:
   - **Tools → Serial Monitor** ή
   - **Ctrl+Shift+M** (Windows/Linux) / **Cmd+Shift+M** (Mac)
4. Ρυθμίστε baud rate στα **9600** (κάτω δεξιά)
5. Στείλτε `1` για να ανάψει το LED
6. Στείλτε `0` για να σβήσει το LED

## Επεξήγηση Κώδικα

### Πλήρης Κώδικας με Αναλυτικά Σχόλια

```cpp
const int LED_PIN = 9;  // Εξωτερικό LED (ή LED_BUILTIN για Pin 13)

void setup() {
  // ΒΗΜΑ 1: Ρύθμιση LED ως έξοδος (πρώτα το hardware)
  pinMode(LED_PIN, OUTPUT);
  
  // ΒΗΜΑ 2: Εκκίνηση σειριακής επικοινωνίας
  Serial.begin(9600);  // 9600 baud = ταχύτητα επικοινωνίας
  
  // ΒΗΜΑ 3: Μηνύματα καλωσορίσματος
  Serial.println("--- Serial LED Control ---");
  Serial.println("Type '1' to turn ON");
  Serial.println("Type '0' to turn OFF");
}

void loop() {
  // ΒΗΜΑ 4: Έλεγχος αν υπάρχουν δεδομένα
  if (Serial.available() > 0) {  // > 0 = υπάρχει τουλάχιστον 1 byte
    
    // ΒΗΜΑ 5: Διάβασμα ενός χαρακτήρα
    char command = Serial.read();  // Διαβάζει 1 byte από τη σειρά
    
    // ΒΗΜΑ 6: Επεξεργασία εντολής
    if (command == '1') {          // Σύγκριση με χαρακτήρα '1' (όχι αριθμό 1!)
      digitalWrite(LED_PIN, HIGH);
      Serial.println("LED turned ON");  // Επιβεβαίωση στο Serial Monitor
    }
    else if (command == '0') {     // Αν πληκτρολογήθηκε '0'
      digitalWrite(LED_PIN, LOW);
      Serial.println("LED turned OFF");
    }
    // Σημείωση: Αγνοούμε άλλους χαρακτήρες (όπως Enter/Newline)
  }
  
  // Το loop συνεχίζει να τρέχει και ελέγχει για νέα δεδομένα
}
```

### Λεπτομερής Ανάλυση Βασικών Εντολών

#### 1️⃣ `Serial.begin(9600)`
```cpp
Serial.begin(9600);  // Εκκίνηση με 9600 baud
```
- Πρέπει να καλείται **μία φορά** στο `setup()`
- Ανοίγει τη σειριακή θύρα με συγκεκριμένη ταχύτητα
- Χωρίς αυτό, το Arduino δεν μπορεί να επικοινωνήσει!

#### 2️⃣ `Serial.available()`
```cpp
if (Serial.available()) {  // Υπάρχουν δεδομένα;
  // ...
}
```
- Επιστρέφει **αριθμό bytes** που περιμένουν στο buffer
- `0` = καθόλου δεδομένα (buffer άδειο)
- `> 0` = υπάρχουν δεδομένα προς ανάγνωση

#### 3️⃣ `Serial.read()`
```cpp
char c = Serial.read();  // Διάβασε 1 χαρακτήρα
```
- Διαβάζει **ένα byte** (1 χαρακτήρα) από το buffer
- Επιστρέφει την τιμή ως `int` (0-255)
- Αποθηκεύουμε σε `char` για σύγκριση με χαρακτήρες

⚠️ **Προσοχή:**  
`'1'` (χαρακτήρας) ≠ `1` (αριθμός)  
`'1'` → ASCII value **49**  
`1` → Αριθμός **1**

#### 4️⃣ `Serial.print()` vs `Serial.println()`
```cpp
Serial.print("Hello");    // Εκτύπωση χωρίς newline
Serial.print(" World");   // Συνεχίζει στην ίδια γραμμή
// Output: "Hello World"

Serial.println("Hello");  // Εκτύπωση με newline (\n)
Serial.println("World");  // Νέα γραμμή
// Output:
// "Hello"
// "World"
```

### Βασικές Εντολές:

| Εντολή | Λειτουργία |
|--------|------------|
| `Serial.begin(baud)` | Εκκίνηση σειριακής με baud rate |
| `Serial.available()` | Επιστρέφει αριθμό διαθέσιμων bytes |
| `Serial.read()` | Διαβάζει 1 byte (χαρακτήρας) |
| `Serial.print(data)` | Στέλνει δεδομένα χωρίς newline |
| `Serial.println(data)` | Στέλνει δεδομένα με newline |

## Debugging με Serial Monitor

### Εκτύπωση Μεταβλητών:
```cpp
int sensorValue = analogRead(A0);
Serial.print("Sensor: ");
Serial.println(sensorValue);
// Output: "Sensor: 512"
```

### Formatted Output:
```cpp
Serial.print("Temperature: ");
Serial.print(temp, 2);  // 2 δεκαδικά ψηφία
Serial.println(" °C");
// Output: "Temperature: 23.45 °C"
```

### Πολλαπλές Τιμές (CSV):
```cpp
Serial.print(time);
Serial.print(",");
Serial.print(value1);
Serial.print(",");
Serial.println(value2);
// Output: "1234,512,768" (εύκολο για Excel/plotter)
```

## Πειραματισμός

### 1. Πολλαπλές Εντολές:
```cpp
if (c == '1') digitalWrite(LED_BUILTIN, HIGH);
if (c == '0') digitalWrite(LED_BUILTIN, LOW);
if (c == 't') digitalWrite(LED_BUILTIN, !digitalRead(LED_BUILTIN)); // Toggle
if (c == '?') Serial.println("Commands: 1=ON, 0=OFF, t=Toggle");
```

### 2. Διάβασμα Αριθμών:
```cpp
if (Serial.available()) {
  int number = Serial.parseInt();  // Διαβάζει ακέραιο
  Serial.print("You sent: ");
  Serial.println(number);
}
```

### 3. Διάβασμα Strings:
```cpp
if (Serial.available()) {
  String command = Serial.readStringUntil('\n');
  
  if (command == "ON") {
    digitalWrite(LED_BUILTIN, HIGH);
  } else if (command == "OFF") {
    digitalWrite(LED_BUILTIN, LOW);
  }
}
```

### 4. Serial Plotter:
```cpp
// Tools → Serial Plotter για γραφικές
void loop() {
  int value = analogRead(A0);
  Serial.println(value);  // Εμφανίζεται ως γράφημα
  delay(10);
}
```

## Troubleshooting

| Πρόβλημα | Λύση |
|----------|------|
| Gibberish characters | Λάθος baud rate - ρυθμίστε 9600 |
| "Port already in use" | Κλείστε Serial Monitor πριν upload |
| Δεν βλέπω output | Ελέγξτε: σωστό port, baud rate, `Serial.begin()` |
| LED δεν αλλάζει | Στείλτε '1' ή '0' (με apostrophes στον κώδικα) |

## Προχωρημένες Τεχνικές

### Parsing Εντολών με Παραμέτρους:
```cpp
// Στείλτε: "PWM:128" για να ρυθμίσετε φωτεινότητα
if (Serial.available()) {
  String cmd = Serial.readStringUntil(':');
  int value = Serial.parseInt();
  
  if (cmd == "PWM") {
    analogWrite(9, value);
    Serial.print("PWM set to ");
    Serial.println(value);
  }
}
```

### Timeout Protection:
```cpp
// Προστασία από κολλημένο Serial.read()
Serial.setTimeout(100);  // 100ms timeout
String input = Serial.readStringUntil('\n');
```

### Non-blocking Serial Read:
```cpp
// Διάβασε μόνο αν υπάρχουν δεδομένα (δεν περιμένει)
void loop() {
  if (Serial.available() > 0) {
    char c = Serial.read();
    // Process command
  }
  
  // Άλλες εργασίες τρέχουν παράλληλα
  digitalWrite(LED_BUILTIN, !digitalRead(LED_BUILTIN));
  delay(500);
}
```

## Σημαντικές Σημειώσεις

### Μνήμη & Strings:
- Το Arduino Uno έχει **2KB RAM**
- Οι `String` καταναλώνουν πολλή μνήμη
- Για production code, προτιμήστε `char[]` arrays

### Buffer Overflow:
```cpp
// Προστασία από υπερχείλιση buffer
const int MAX_CMD_LEN = 32;
char buffer[MAX_CMD_LEN];
int idx = 0;

if (Serial.available() && idx < MAX_CMD_LEN - 1) {
  buffer[idx++] = Serial.read();
}
```

### Πολύ Output = Αργό Πρόγραμμα:
```cpp
// ❌ Αποφύγετε excessive prints στο loop()
void loop() {
  Serial.println(analogRead(A0));  // 9600 baud = αργό!
  // Προσθέστε delay ή throttle
}
```

## Επόμενο Βήμα

Τώρα που γνωρίζετε τα βασικά, ήρθε η ώρα να τα συνδυάσετε όλα μαζί!

Προχωρήστε στο **[09_Basics_Capstone](../09_Basics_Capstone/)** για το τελικό project αυτής της ενότητας.
